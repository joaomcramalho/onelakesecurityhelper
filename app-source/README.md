# Development source

This folder contains the React 19, TypeScript, and Vite source for the OneLake Security Explorer.

```powershell
npm install
npm run dev
```

Available checks:

```powershell
npm test
npm run lint
npm run build
```

The production build creates a self-contained `dist\index.html`. Copy that file to the repository root to update the GitHub Pages site.
